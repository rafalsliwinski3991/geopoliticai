version: "3.9"

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      LOG_LEVEL: INFO
  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
