# Sources Changelog

Append-only record of changes to `ENGLISH_INFOSPHERE_SOURCES` and
`POLISH_INFOSPHERE_SOURCES` in `app/src/geopoliticai/config.py`. Bump
`SOURCES_VERSION` on every change.

See `docs/sources-methodology.md` for the selection criteria and review
process.

## SOURCES_VERSION = "2026-Q2"

- Initial versioned snapshot. See `config.py` for the full lists at this
  version. No additions or removals — this entry simply marks the point
  at which the lists became formally tracked.
